// Test constants for e2e tests
// These should match the values in src/config.ts
export const TEST_CONSTANTS = {
	MAX_CHARACTERS: 1200,
	APP_NAME: 'Reflect',
	APP_DESCRIPTION: 'Share your thoughts in markdown format'
} as const;
